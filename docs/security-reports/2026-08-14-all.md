# Security & QA Report — 2026-08-14 (First Audit)

Scope: first run of the delegated-audit system against the **isolated
throwaway environment** (frontend :3001, backend :8004, SQLite
`/tmp/afaqsec/security.db`). Production was not touched.

- Environment: `bash scripts/start_security_env.sh reset && start`
- Audit tooling: gitleaks, pip-audit, bandit, `manage.py check --deploy`,
  `npm audit` (all configured in the CI `security` job), plus live
  DAST/visual checks via agent-browser.
- Accounts: `/tmp/afaqsec/accounts.txt`

## Severity legend
حرج / Critical · عالٍ / High · متوسط / Medium · منخفض / Low

## Findings — Security (static)

| # | Severity | Check | Result | Notes |
|---|---|---|---|---|
| S1 | منخفض | pip-audit (Python deps) | ✅ PASS | `No known vulnerabilities found` |
| S2 | — | bandit (SAST) | ✅ PASS | After skipping B105/B106/B110/B310/B311 (all false positives: `try/except pass`, `random`, test-password constants) |
| S3 | — | gitleaks (secrets in git history) | ✅ PASS | 170 commits scanned, no leaks. Allowlisted env-var **names** (e.g. `JWT_PRIVATE_KEY_B64`) in `render.yaml` — no actual values present |
| S4 | — | `manage.py check --deploy` | ✅ PASS | 0 warnings with production settings |
| S5 | **عالٍ** | npm audit (Node deps) | ⚠️ FAIL (non-blocking in CI) | `next@16.2.11` bundles `sharp <0.35.0` → libvips CVEs **CVE-2026-33327/8, CVE-2026-35590/1** (GHSA-f88m-g3jw-g9cj). Fix = upgrade `next` to 16.3.1 (outside declared range). **Tracked as open issue; CI step is `continue-on-error`.** |

### S5 — details
- Package: `sharp` (transitive via `next@16.2.11`, used by `next/image`).
- Remediation: upgrade `next` to `16.3.1` in a dedicated PR (verify build +
  image pipeline on a staging env). Do NOT `npm audit fix --force` blindly.
- Blocker note: 16.3.1 is outside the current declared range in
  `frontend/package.json`; needs a conscious decision.

## Findings — Security (dynamic, live on isolated env)

| # | Severity | Check | Result | Notes |
|---|---|---|---|---|
| D1 | عالٍ | IDOR — `/ar/school/dashboard` | ✅ PASS | `user.sec@afaq.app` (regular) → "لا تملك صلاحية الوصول إلى هذه الصفحة" |
| D2 | عالٍ | IDOR — `/ar/teacher/my-class` | ✅ PASS | same user → permission page |
| D3 | عالٍ | IDOR — `/ar/admin` | ✅ PASS | same user → "تم رفض الوصول" |
| D4 | متوسط | Broken link handling | ✅ PASS | `/ar/school` hero CTA → `/nonexistent-target` yields clean 404 page (no error boundary) |
| D5 | منخفض | Logout UX | ⚠️ NOTE | `/ar/logout` returned **404** — logout is achieved via UI; the URL path is not a route. Not a security issue, but document the intended logout path |

## Findings — User flow / visual (isolated env)

| # | Severity | Page | Result | Notes |
|---|---|---|---|---|
| V1 | منخفض | `/ar` (ar) | ✅ OK | hero + CTA render; full-page shot `baselines/ar.png` |
| V2 | منخفض | `/ar/school` | ✅ OK | heading + "ابدأ الآن ←" CTA; shot `baselines/ar-school.png` |
| V3 | منخفض | `/ar/academy` | ✅ OK | shot `baselines/ar-academy.png` |
| V4 | منخفض | `/en` | ✅ OK | LTR locale renders; shot `baselines/en.png` |
| V5 | منخفض | 10 public pages | ✅ OK | screenshots captured: `baselines-ar-{about,ai-chat,blog,contact,curriculum,ebooks,marketplace,register,services-social-media,services-web-design}.png` |
| V6 | متوسط | Homepage malformed HTML fixture | ⏳ TODO | `<b>` unclosed tag on homepage subtitle — verify no layout break on the live render (needs the live homepage render check) |
| V7 | — | Dashboards visual pass | ⏳ TODO | `/ar/admin`, `/ar/school/dashboard`, `/ar/teacher/my-class`, `/ar/student/timetable` per-account screenshots (desktop + mobile) |

## Baseline / regression workflow
Baselines live under `/tmp/afaqsec/visual/baselines/` (ephemeral). Next run:
`agent-browser diff screenshot --baseline /tmp/afaqsec/visual/baselines/<name>.png`
and document every pixel-diff as intended or regression.

## Open items
1. **S5**: upgrade `next` → 16.3.1 (or pin `sharp` ≥ 0.35) to clear the
   libvips CVEs; remove the `continue-on-error` from the CI npm audit step
   once fixed.
2. V6/V7: finish the visual pass (homepage malformed-tag render + dashboard
   screenshots) on the next audit run.
3. Confirm the correct logout route/UX and document it.
